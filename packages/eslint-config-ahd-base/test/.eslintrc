{
  "rules": {
    // disabled because I find it tedious to write test while following this rule
    "no-shadow": 0,
    // test uses `t` for tape
    "id-length": [2, {"min": 2, "properties": "never", "exceptions": ["t"]}]
  }
}
